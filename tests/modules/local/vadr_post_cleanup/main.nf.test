nextflow_process {

    name "Test Process VADR_POST_CLEANUP"
    script "modules/local/vadr_post_cleanup/main.nf"
    process "VADR_POST_CLEANUP"

    test("Should run without failures") {

        when {
            params {
                // define parameters here.
            }
            process {
                """
                input[0] =  [ [ batch_id: "batch_1",  batch_tsv: file("${projectDir}/tests/modules/local/repeatmasker_annotation/batch_1.tsv"), sample_id: "NY0006" ], 
                               file("${projectDir}/tests/modules/local/vadr_post_cleanup/NY0006_mpxv/") 
                            ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
