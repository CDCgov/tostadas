nextflow_process {

    name "Test Process VADR_TRIM"
    script "modules/local/vadr_trim/main.nf"
    process "VADR_TRIM"

    test("Should run without failures") {

        when {
            params {
                prefix = "NY0006"
            }
            process {
                """
                input[0] = [ [ batch_id: "batch_1",  batch_tsv: file("${projectDir}/tests/modules/local/prep_submission/batch_1.tsv"), sample_id: "NY0006" ], 
                            file("${projectDir}/assets/sample_fastas/mpox/NY0006.fasta"), 
                           ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }
    }

}
